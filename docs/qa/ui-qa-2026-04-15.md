# UI QA Evidence - 2026-04-15

## Scope

This round focused on:

- homepage and shared front-end narrative text
- admin localization and operations-shell standardization
- admin page consistency for login, dashboard, content management, AI logs, and placeholders

## QA Inventory

### Front-end claims checked

- Homepage hero should no longer display garbled question-mark text.
- Shared chapter labels, hero notes, and narrative captions should return valid Chinese text from the API.
- AI pages should remain buildable after shared text-source cleanup.

### Admin claims checked

- Admin shell should be fully Chinese-localized and visually consistent.
- Login page, dashboard, content pages, banner page, AI logs, and placeholder pages should all use the same operations-console tone.
- Common table, dialog, form, and card treatments should look like one system instead of mixed defaults.

## Evidence Captured

### Live service checks

- `http://127.0.0.1:4173/` reachable
- `http://127.0.0.1:4174/admin/login` reachable
- `http://127.0.0.1:3000/api/front/home` reachable

### Front-end API assertions

Verified from live API response:

- `hero.title = 赣州长卷`
- `hero.note = 先形成城市气质，再进入章节、地点与 AI 导览。`
- `visualSystem.thesis = 夜色长卷、城市灯影、低饱和纸面与克制高光。`
- first chapter caption and route label are valid Chinese

### Build checks

- `admin-web` production build passed after shell, routing, login, dashboard, and style updates
- `client-web` production build passed after shared narrative-source cleanup
- `server/src/services/home.service.js` syntax check passed
- `server/src/utils/front-view-models.js` syntax check passed

## Real Browser QA Status

### Intended method

User requested `[$playwright-interactive](C:/Users/Administrator/.codex/skills/playwright-interactive/SKILL.md)`.

### Limitation encountered

This Codex session does not expose `js_repl`, which is a hard prerequisite for the skill. Because of that, the persistent Playwright workflow could not be executed inside this session.

### Fallback used

- live service verification
- production builds
- source-level mojibake scans
- browser-launch verification for local pages

## Findings Fixed In This Round

- Replaced garbled shared narrative text sources on both client and server
- Added dirty-data fallback in home service so old database values do not reintroduce question-mark text
- Rewrote admin shell, login, dashboard, router meta, and remaining placeholder pages in clean Chinese
- Standardized Element Plus presentation for cards, dialogs, tables, forms, buttons, and pagination

## Residual QA Gap

- A full persistent-browser walkthrough of authenticated admin pages is still worth doing in a future session with `js_repl` enabled, especially for mobile-fit and post-login visual polish evidence.
